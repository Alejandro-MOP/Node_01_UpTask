<!DOCTYPE html>
html(lang="es")
    head
        meta(charset="UTF-8")
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
            
        
    body
        h2 Reestablecer Contraseña
        p Hola, has solicitado reestablecer tu contraseña, haz click en el siguiente enlace para reestablecerlo, este enlace es temporal, así que si este se vence vuelve a solicitarlo por favor.
        a(href=`${resetUrl}`) Ir a recuperar contraseña.
        p Si no puedes acceder a este enlace, visita #{resetUrl}
        p Si no solicitaste este email, puedes ignorarlo.